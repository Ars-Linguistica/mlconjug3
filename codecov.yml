coverage:
  status:
    project:
      default:
        target: 80%
        threshold: 0%
        informational: false

    patch:
      default:
        target: 80%
        threshold: 0%
        informational: false

ignore:
  - "**/*.yaml"
  - "**/*.yml"
