name: Build & Test (Poetry)

on:
  push:
  pull_request:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: ${{ matrix.os }} • Python ${{ matrix.python-version }} • ${{ matrix.sklearn_label }}
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        python-version: ["3.8", "3.9", "3.10", "3.11", "3.12", "3.13", "3.14"]
        sklearn:
          - ">=1.2,<1.4"
          - ">=1.3,<2"

        include:
          - sklearn: ">=1.2,<1.4"
            sklearn_label: "sk12"
          - sklearn: ">=1.3,<2"
            sklearn_label: "sk13plus"

        exclude:
          - python-version: "3.8"
            sklearn: ">=1.3,<2"

    steps:
      - name: Harden Runner
        uses: step-security/harden-runner@v2
        with:
          egress-policy: audit

      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: "pip"

      - name: Install Poetry
        uses: snok/install-poetry@v1
        with:
          version: "1.8.3"   # ✅ FIXED
          virtualenvs-create: true
          virtualenvs-in-project: true
          installer-parallel: true

      - name: Cache Poetry virtualenv
        uses: actions/cache@v4
        with:
          path: .venv
          key: venv-${{ runner.os }}-${{ matrix.python-version }}-${{ matrix.sklearn_label }}-${{ hashFiles('poetry.lock') }}

      - name: Install dependencies
        run: poetry install --no-interaction

      - name: Override scikit-learn version (matrix)
        run: poetry run pip install "scikit-learn${{ matrix.sklearn }}"

      - name: Show environment info
        run: |
          poetry run python --version
          poetry run pip list

      - name: Run type checking
        run: |
          poetry run pip install types-setuptools
          poetry run mypy

      - name: Run tests
        run: poetry run pytest --cov=mlconjug3 --cov-report=xml

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v4
        with:
          files: ./coverage.xml

  build:
    name: Build package
    runs-on: ubuntu-latest
    needs: test

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Poetry
        uses: snok/install-poetry@v1
        with:
          version: "1.8.3"   # ✅ FIXED

      - name: Build package
        run: poetry build
